specVersion: "v1"
serviceId: "ifmakersite"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "ifmakersite-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
